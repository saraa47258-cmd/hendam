# 🎨 تصميم صفحة المنتج لاختيار القماش

## 🎯 **الهدف:**
تطبيق تصميم احترافي مشابه لصفحات المنتجات في المتاجر الإلكترونية مع قائمة منسدلة لاختيار اللون.

---

## ✨ **التصميم الجديد:**

### 1. **قائمة الأقمشة (List View)**

تصميم بسيط وسريع لعرض جميع الأقمشة المتاحة:

```
┌────────────────────────────────────┐
│ ┌──────┐  قطن ياباني فاخر         │
│ │      │  ر.ع 5.950               │
│ │ صورة │  ⭕⭕⭕⭕                │
│ │      │                           │
│ └──────┘                           │
├────────────────────────────────────┤
│ ┌──────┐  حرير فاخر               │
│ │      │  ر.ع 12.000              │
│ │ صورة │  ⭕⭕⭕                  │
│ │      │                           │
│ └──────┘                           │
└────────────────────────────────────┘
```

**المميزات:**
- ✅ صورة صغيرة 80×80
- ✅ اسم القماش (عريض)
- ✅ السعر (بلون مميز)
- ✅ دوائر الألوان المتاحة (4 maximum)
- ✅ علامة تحديد عند الاختيار

---

### 2. **البطاقة التفصيلية (Product Card)**

عند اختيار قماش، تظهر بطاقة تفصيلية مشابهة لصفحة المنتج:

```
┌────────────────────────────────────────┐
│  ✓ قطن ياباني فاخر                   │
│                                        │
│  ر.ع 5.950  ⚊ر.ع 7.438⚊             │
│                                        │
│  ╔════════════════════════════════╗   │
│  ║ ℹ️  قطن فاخر بجودة عالية...   ║   │
│  ╚════════════════════════════════╝   │
│                                        │
│  اللون *                               │
│  ┌──────────────────────────────┐     │
│  │ ⭕ كريمي              ▼      │     │
│  └──────────────────────────────┘     │
│  اختر                                 │
└────────────────────────────────────────┘
```

**المميزات:**
- ✅ عنوان كبير مع علامة ✓
- ✅ السعر الحالي والسعر الأصلي (إن وجد خصم)
- ✅ وصف في صندوق ملون
- ✅ قائمة منسدلة للألوان **← الميزة الجديدة!**
- ✅ نص "اختر" تحت القائمة

---

## 🎨 **القائمة المنسدلة للألوان:**

### التصميم:

```dart
class _ColorDropdown extends StatefulWidget {
  final List<dynamic> colors;
  final Function(Map<String, dynamic>) onColorSelected;
  // ...
}
```

### المظهر:

```
┌─────────────────────────────────────┐
│  اختر اللون                    ▼   │  ← القائمة المغلقة
└─────────────────────────────────────┘

عند الفتح:
┌─────────────────────────────────────┐
│  اختر اللون                    ▲   │
├─────────────────────────────────────┤
│  ⭕ كريمي                          │
│  ⭕ أبيض                           │
│  ⭕ بيج                            │
│  ⭕ رمادي                          │
│  ⭕ أسود                           │
└─────────────────────────────────────┘
```

### كل عنصر في القائمة يحتوي على:

```
┌─────────────────────────────┐
│  ⭕  اسم اللون              │
│   ↑                         │
│   دائرة ملونة (32×32)       │
│   مع ظل وحدود              │
└─────────────────────────────┘
```

---

## 📱 **آلية العمل:**

### 1. العرض الأولي:
```
المستخدم يدخل الصفحة
  ↓
يرى قائمة بسيطة بجميع الأقمشة
  ↓
كل قماش: صورة + اسم + سعر + ألوان
```

### 2. اختيار القماش:
```
المستخدم ينقر على قماش
  ↓
تظهر البطاقة التفصيلية في الأعلى
  ↓
القائمة تبقى في الأسفل لتغيير الاختيار
```

### 3. اختيار اللون:
```
المستخدم ينقر على القائمة المنسدلة
  ↓
تظهر جميع الألوان المتاحة
  ↓
كل لون: دائرة ملونة + اسم
  ↓
عند الاختيار: يتم حفظ اللون المختار
```

---

## 🎨 **الكود الأساسي:**

### أ) بطاقة القماش البسيطة:

```dart
InkWell(
  onTap: () => widget.onTypeChanged(...),
  child: Container(
    padding: EdgeInsets.all(12),
    decoration: BoxDecoration(
      borderRadius: BorderRadius.circular(12),
      border: Border.all(
        color: selected ? cs.primary : cs.outlineVariant,
        width: selected ? 2 : 1,
      ),
    ),
    child: Row(
      children: [
        // صورة 80x80
        ClipRRect(
          borderRadius: BorderRadius.circular(8),
          child: SizedBox(
            width: 80,
            height: 80,
            child: Image...
          ),
        ),
        
        // المعلومات
        Expanded(
          child: Column(
            children: [
              Text(fabricName),
              Text(price),
              Row([دوائر الألوان]),
            ],
          ),
        ),
      ],
    ),
  ),
)
```

### ب) البطاقة التفصيلية:

```dart
Widget buildSelectedFabricDetailCard(fabric) {
  return Container(
    padding: EdgeInsets.all(20),
    decoration: BoxDecoration(
      border: Border.all(color: cs.primary, width: 2),
      borderRadius: BorderRadius.circular(16),
    ),
    child: Column(
      children: [
        // العنوان + علامة ✓
        Row([Text(name), Icon(check)]),
        
        // السعر
        Row([سعر حالي, سعر أصلي مشطوب]),
        
        // الوصف
        Container([Icon, Text(description)]),
        
        // القائمة المنسدلة
        Text('اللون *'),
        _ColorDropdown(
          colors: availableColors,
          onColorSelected: (color) {
            // معالجة اختيار اللون
          },
        ),
        Text('اختر'),
      ],
    ),
  );
}
```

### ج) القائمة المنسدلة:

```dart
class _ColorDropdown extends StatefulWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(8),
        border: Border.all(color: cs.outlineVariant),
      ),
      child: DropdownButton<Map<String, dynamic>>(
        hint: Text('اختر اللون'),
        items: colors.map((color) {
          return DropdownMenuItem(
            value: color,
            child: Row([
              // دائرة اللون
              Container(
                width: 32,
                height: 32,
                decoration: BoxDecoration(
                  shape: BoxShape.circle,
                  color: colorValue,
                  border: Border.all(...),
                  boxShadow: [BoxShadow(...)],
                ),
              ),
              SizedBox(width: 12),
              // اسم اللون
              Text(colorName),
            ]),
          );
        }).toList(),
        onChanged: (color) {
          setState(() => _selectedColor = color);
          widget.onColorSelected(color);
        },
      ),
    );
  }
}
```

---

## 🎨 **التصميم البصري:**

### الألوان:
```
الخلفية: cs.surface (أبيض)
الحدود: cs.outlineVariant (رمادي فاتح)
الحدود النشطة: cs.primary (أزرق)
السعر: cs.primary أو cs.error (للخصم)
الوصف: cs.surfaceContainerHighest (رمادي فاتح جداً)
```

### الظلال:
```
البطاقة المحددة:
  color: cs.primary.withOpacity(0.15)
  blurRadius: 16
  offset: Offset(0, 4)

دائرة اللون:
  color: color.withOpacity(0.3)
  blurRadius: 4
  offset: Offset(0, 2)
```

### الحواف:
```
البطاقة الرئيسية: 16
البطاقات البسيطة: 12
الصورة: 8
القائمة المنسدلة: 8
```

### الأحجام:
```
الصورة الصغيرة: 80×80
دائرة اللون الصغيرة: 18×18
دائرة اللون في القائمة: 32×32
علامة التحديد: 14-20
```

---

## 📊 **المقارنة:**

| العنصر | قبل | بعد |
|--------|-----|-----|
| **عرض الأقمشة** | شبكة معقدة | قائمة بسيطة |
| **تفاصيل القماش** | مدمجة في البطاقة | بطاقة منفصلة كبيرة |
| **اختيار اللون** | دوائر ثابتة | قائمة منسدلة ✓ |
| **السعر** | عادي | مع خصم (إن وجد) |
| **الوصف** | نص بسيط | صندوق ملون |
| **الأداء** | متوسط | سريع جداً |

---

## ✅ **المميزات:**

### 1. **قائمة منسدلة احترافية:**
- ✅ دائرة ملونة لكل لون
- ✅ اسم اللون بجانب الدائرة
- ✅ ظل ملون لكل دائرة
- ✅ أيقونة للألوان الفاتحة جداً
- ✅ تصميم Material Design

### 2. **بطاقة تفصيلية:**
- ✅ عنوان كبير وواضح
- ✅ سعر بارز مع خصم
- ✅ وصف في صندوق ملون
- ✅ تصميم احترافي

### 3. **قائمة بسيطة:**
- ✅ صورة + اسم + سعر
- ✅ دوائر الألوان المتاحة
- ✅ علامة تحديد واضحة
- ✅ أداء سريع

### 4. **تجربة مستخدم ممتازة:**
- ✅ عرض مختصر للتصفح السريع
- ✅ عرض تفصيلي عند الاختيار
- ✅ قائمة منسدلة سهلة الاستخدام
- ✅ تنقل سلس

---

## 🎯 **النتيجة:**

**تصميم احترافي مشابه للمتاجر الإلكترونية مع قائمة منسدلة أنيقة لاختيار الألوان! 🚀**

المستخدم الآن يمكنه:
1. ✅ تصفح جميع الأقمشة بسرعة
2. ✅ رؤية التفاصيل الكاملة عند الاختيار
3. ✅ اختيار اللون من قائمة منسدلة احترافية
4. ✅ رؤية اللون بوضوح (دائرة + اسم)
5. ✅ الاستمتاع بتجربة سلسة وسريعة

**التطبيق جاهز! 🎉**

