# ✅ ميزة عرض تصاميم التطريز المتاحة

## 🎯 **الميزة الجديدة:**

تم إضافة نظام كامل لعرض تصاميم التطريز المتاحة من Firebase Storage لكل خياط، مع إمكانية اختيارها وحفظها في الطلب.

## 📂 **هيكل Firebase Storage:**

```
firebase-storage-bucket/
└── tailors/
    └── {tailorId}/
        └── embroidery_images/
            ├── 1699876543210.jpg  ← صورة 1
            ├── 1699876789456.jpg  ← صورة 2
            └── 1699877012345.jpg  ← صورة 3
```

**أو بديلاً في Firestore:**

```
firestore/
└── tailors/
    └── {tailorId}/
        └── embroideryDesigns/
            ├── {designId1}
            │   ├── imageUrl: "https://..."
            │   ├── name: "تطريز زهور"
            │   ├── price: 2.500
            │   └── uploadedAt: timestamp
            ├── {designId2}
            └── {designId3}
```

## 🆕 **الملفات الجديدة:**

### 1. **نموذج التطريز** - `lib/features/tailors/models/embroidery_design.dart`

```dart
class EmbroideryDesign {
  final String id;
  final String imageUrl;
  final String name;
  final double price;
  final DateTime uploadedAt;
  
  // Methods:
  // - fromMap()
  // - toMap()
  // - copyWith()
}
```

**المميزات:**
- ✅ تخزين معلومات التصميم
- ✅ التحويل من/إلى Firestore
- ✅ نسخ مع تعديلات

### 2. **خدمة التطريز** - `lib/features/tailors/services/embroidery_service.dart`

```dart
class EmbroideryService {
  // جلب التصاميم من Firestore أو Storage
  Future<List<EmbroideryDesign>> getEmbroideryDesigns(String tailorId);
  
  // Stream للتحديثات المباشرة
  Stream<List<EmbroideryDesign>> streamEmbroideryDesigns(String tailorId);
  
  // حفظ تصميم جديد
  Future<void> saveEmbroideryDesign(String tailorId, EmbroideryDesign design);
  
  // حذف تصميم
  Future<void> deleteEmbroideryDesign(String tailorId, String designId);
  
  // تحديث تصميم
  Future<void> updateEmbroideryDesign(String tailorId, String designId, Map updates);
}
```

**المميزات:**
- ✅ جلب من Firestore أو Storage
- ✅ Stream للتحديثات المباشرة
- ✅ CRUD operations كاملة
- ✅ معالجة الأخطاء

### 3. **UI Component** - `_EmbroideryDesignsSection`

```dart
class _EmbroideryDesignsSection extends StatelessWidget {
  final String tailorId;
  final EmbroideryDesign? selectedEmbroidery;
  final ValueChanged<EmbroideryDesign?> onEmbroiderySelected;
  
  @override
  Widget build(BuildContext context) {
    return FutureBuilder<List<EmbroideryDesign>>(...);
  }
}
```

**التصميم:**

```
┌─────────────────────────────────────┐
│  ✨ تصاميم التطريز المتاحة         │
│  اختر تصميم التطريز المفضل لديك    │
│                                     │
│  ┌───┐  ┌───┐  ┌───┐              │
│  │   │  │ ✓ │  │   │              │
│  │ 1 │  │ 2 │  │ 3 │              │
│  │   │  │   │  │   │              │
│  └───┘  └───┘  └───┘              │
│  تطريز  تطريز  تطريز              │
│  +2.5   +3.0   +1.5               │
└─────────────────────────────────────┘
```

**المميزات:**
- ✅ Grid view 3 أعمدة
- ✅ صور من Firebase Storage
- ✅ علامة اختيار (✓) عند التحديد
- ✅ حدود وظلال للمختار
- ✅ عرض السعر الإضافي
- ✅ Haptic feedback
- ✅ حالات التحميل والأخطاء

## 🔄 **التحديثات على الملفات الموجودة:**

### 1. **OrderModel** - `lib/features/orders/models/order_model.dart`

**الحقول الجديدة:**
```dart
// معلومات التطريز
final String? embroideryDesignId;
final String? embroideryDesignName;
final String? embroideryDesignImageUrl;
final double? embroideryDesignPrice;
```

**التحديثات:**
- ✅ Constructor محدث
- ✅ `fromFirestore()` محدث
- ✅ `toFirestore()` محدث
- ✅ `copyWith()` محدث

### 2. **TailoringDesignScreen** - `lib/features/tailors/presentation/tailoring_design_screen.dart`

**المتغيرات الجديدة:**
```dart
EmbroideryDesign? _selectedEmbroidery; // التطريز المختار
```

**تحديث التسعير:**
```dart
double get _price {
  double p = widget.basePriceOMR;
  // ... أسعار أخرى ...
  
  // إضافة سعر تصميم التطريز المختار
  if (_selectedEmbroidery != null && _selectedEmbroidery!.price > 0) {
    p += _selectedEmbroidery!.price;
  }
  
  return p;
}
```

**تحديث _EmbroideryStep:**
```dart
_EmbroideryStep(
  color: _embroideryColor,
  addName: _addNameEmbroidery,
  lines: _embroideryLines,
  onChanged: (color, addName, lines) => setState(...),
  tailorId: widget.tailorId,                    // جديد ✨
  selectedEmbroidery: _selectedEmbroidery,     // جديد ✨
  onEmbroideryChanged: (design) => setState(...), // جديد ✨
),
```

**تحديث submitOrder:**
```dart
embroideryDesignId: _selectedEmbroidery?.id,
embroideryDesignName: _selectedEmbroidery?.name,
embroideryDesignImageUrl: _selectedEmbroidery?.imageUrl,
embroideryDesignPrice: _selectedEmbroidery?.price,
```

## 📱 **كيفية الاستخدام:**

### 1. **رفع الصور إلى Firebase Storage:**

```javascript
// في Firebase Console أو من التطبيق
const path = `tailors/${tailorId}/embroidery_images/${timestamp}.jpg`;
await storage.ref(path).putFile(imageFile);
```

### 2. **أو إضافة البيانات في Firestore:**

```javascript
// في Firebase Console
firestore.collection('tailors').doc(tailorId)
  .collection('embroideryDesigns').doc(designId).set({
    imageUrl: 'https://...',
    name: 'تطريز زهور',
    price: 2.500,
    uploadedAt: Date.now()
  });
```

### 3. **في التطبيق:**

```dart
// 1. المستخدم يصل لخطوة التطريز
// 2. يظهر قسم "تصاميم التطريز المتاحة"
// 3. يختار تصميم → تتحدث الحدود والسعر
// 4. عند الإرسال → يحفظ في الطلب
```

## ✅ **المميزات:**

### 1. **جلب ذكي:**
- ✅ يبحث أولاً في Firestore
- ✅ إذا لم يجد → يجلب من Storage
- ✅ Stream للتحديثات المباشرة

### 2. **UI احترافي:**
- ✅ Grid view responsive
- ✅ صور من CachedNetworkImage
- ✅ حالات loading/error/empty
- ✅ تصميم جميل مع gradients
- ✅ Haptic feedback

### 3. **التسعير:**
- ✅ السعر يتحدث تلقائياً
- ✅ يظهر في شريط السعر السفلي
- ✅ يُحفظ مع الطلب

### 4. **المرونة:**
- ✅ التطريز اختياري (nullable)
- ✅ يمكن إلغاء الاختيار
- ✅ كل خياط له تصاميمه الخاصة

## 📊 **مثال كامل:**

### السيناريو:
```
1. الخياط "محمد" يرفع 3 تصاميم تطريز:
   - تطريز زهور (2.500 ر.ع)
   - تطريز هندسي (3.000 ر.ع)
   - تطريز بسيط (1.500 ر.ع)

2. العميل "أحمد" يفتح صفحة التصميم:
   - يختار القماش
   - يدخل المقاسات
   - ينتقل لخطوة التطريز
   
3. يظهر قسم "تصاميم التطريز المتاحة":
   ┌───────────────────────────────────┐
   │  [صورة]  [صورة]  [صورة]        │
   │  زهور    هندسي   بسيط           │
   │  +2.5    +3.0     +1.5           │
   └───────────────────────────────────┘

4. أحمد يختار "تطريز زهور":
   - يظهر ✓ على الصورة
   - حدود زرقاء مع ظل
   - السعر يتحدث: 6.000 → 8.500

5. عند الإرسال:
   - يُحفظ في الطلب:
     * embroideryDesignId: "1699876543210"
     * embroideryDesignName: "تطريز زهور"
     * embroideryDesignImageUrl: "https://..."
     * embroideryDesignPrice: 2.500
```

## 🔍 **معالجة الحالات:**

### 1. **لا توجد تصاميم:**
```
┌─────────────────────────────────────┐
│  ℹ️  لا توجد تصاميم تطريز متاحة    │
│     حالياً                          │
└─────────────────────────────────────┘
```

### 2. **جاري التحميل:**
```
┌─────────────────────────────────────┐
│            ⏳                        │
└─────────────────────────────────────┘
```

### 3. **خطأ:**
```
┌─────────────────────────────────────┐
│  ❌ حدث خطأ في تحميل تصاميم        │
│     التطريز                         │
└─────────────────────────────────────┘
```

## 🎯 **النتيجة النهائية:**

**نظام كامل ومتكامل لتصاميم التطريز! 🚀**

- ✅ جلب من Firebase Storage/Firestore
- ✅ UI احترافي وجميل
- ✅ اختيار وحفظ في الطلب
- ✅ تحديث السعر تلقائياً
- ✅ معالجة جميع الحالات
- ✅ كل خياط له تصاميمه
- ✅ مرن واختياري

**التطبيق جاهز! ✨**

