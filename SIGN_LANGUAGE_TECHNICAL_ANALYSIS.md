# 🔬 تحليل تقنية كشف الإشارات في المشروع

## 📊 نظرة عامة

المشروع يستخدم **MediaPipe Hand Landmarker** من Google لكشف إشارات اليد وتحويلها إلى نص.

## 🏗️ المعمارية التقنية

```
┌─────────────────────────────────────────────────────────────┐
│                    Flutter (Dart)                            │
│  ┌────────────────────────────────────────────────────┐     │
│  │  sign_language_camera_page.dart                    │     │
│  │  - التقاط الصور من الكاميرا                       │     │
│  │  - تحويل YUV → JPEG                               │     │
│  │  - عرض النتائج                                    │     │
│  └─────────────────────┬──────────────────────────────┘     │
│                        │                                     │
│  ┌────────────────────▼──────────────────────────────┐     │
│  │  sign_language_detector.dart                       │     │
│  │  - MethodChannel للتواصل مع Native                │     │
│  └─────────────────────┬──────────────────────────────┘     │
└────────────────────────┼──────────────────────────────────────┘
                         │ MethodChannel
                         │ 'com.example.hindam/hand_gesture'
┌────────────────────────▼──────────────────────────────────────┐
│                 Android (Kotlin)                              │
│  ┌────────────────────────────────────────────────────┐      │
│  │  MainActivity.kt                                    │      │
│  │  - تسجيل MethodChannel                             │      │
│  │  - توجيه الطلبات                                   │      │
│  └─────────────────────┬──────────────────────────────┘      │
│                        │                                      │
│  ┌────────────────────▼──────────────────────────────┐      │
│  │  HandGestureHandler.kt                             │      │
│  │  - تهيئة MediaPipe                                 │      │
│  │  - معالجة الصور                                    │      │
│  │  - تصنيف الإشارات                                  │      │
│  └─────────────────────┬──────────────────────────────┘      │
│                        │                                      │
│  ┌────────────────────▼──────────────────────────────┐      │
│  │  MediaPipe Hand Landmarker                         │      │
│  │  - نموذج ML: hand_landmarker.task (7 MB)         │      │
│  │  - كشف 21 نقطة لليد                               │      │
│  │  - إحداثيات (x, y, z) لكل نقطة                   │      │
│  └────────────────────────────────────────────────────┘      │
└───────────────────────────────────────────────────────────────┘
```

## 🎯 آلية العمل التفصيلية

### 1️⃣ التقاط الصورة (Flutter)
```dart
// في sign_language_camera_page.dart
CameraController.startImageStream((CameraImage image) {
  // يلتقط 30 إطار/ثانية
  // يعالج إطار واحد من كل 15 للأداء
})
```

**التحديات:**
- تنسيق الصورة: `YUV420` من الكاميرا
- يحتاج تحويل إلى RGB ثم JPEG

### 2️⃣ تحويل الصورة
```dart
Future<Uint8List?> _convertToJpeg(CameraImage image) {
  // YUV → RGB → JPEG
  // استخدام مكتبة image
}
```

**المعادلات:**
```
R = Y + 1.402 * (V - 128)
G = Y - 0.344 * (U - 128) - 0.714 * (V - 128)
B = Y + 1.772 * (U - 128)
```

### 3️⃣ إرسال للمعالجة (MethodChannel)
```dart
// sign_language_detector.dart
final String result = await platform.invokeMethod('detectHand', {
  'imageBytes': imageBytes,
});
```

### 4️⃣ معالجة في Android (Kotlin)

#### أ. فك تشفير الصورة
```kotlin
val bitmap = BitmapFactory.decodeByteArray(imageBytes, 0, imageBytes.size)
val mpImage = BitmapImageBuilder(bitmap).build()
```

#### ب. كشف اليد
```kotlin
val detectionResult = handLandmarker?.detect(mpImage)
// النتيجة: 21 نقطة × (x, y, z)
```

#### ج. تحليل الأصابع
```kotlin
private fun fingerUp(landmarks, tip, pip): Boolean {
    return landmarks[tip].y() < landmarks[pip].y()
    // إذا طرف الإصبع أعلى من المفصل = مرفوع
}
```

### 5️⃣ التصنيف

```kotlin
when {
    indexUp && !middleUp && !ringUp && !pinkyUp -> "أ"
    indexUp && middleUp && !ringUp && !pinkyUp -> "ب"
    indexUp && middleUp && ringUp && !pinkyUp -> "ج"
    // ... المزيد
}
```

## 📍 نقاط MediaPipe Hand (21 نقطة)

```
        8 - السبابة (Index Tip)
       /
      7
     /
    6 - مفصل السبابة (Index PIP)
   /
  5
 /
0 - المعصم (Wrist)
 \
  1 - قاعدة الإبهام
   \
    2
     \
      3 - مفصل الإبهام (Thumb IP)
       \
        4 - طرف الإبهام (Thumb Tip)

12 - الوسطى (Middle Tip)
10 - مفصل الوسطى (Middle PIP)

16 - البنصر (Ring Tip)
14 - مفصل البنصر (Ring PIP)

20 - الخنصر (Pinky Tip)
18 - مفصل الخنصر (Pinky PIP)
```

## ⚡ الأداء

### معدلات المعالجة:
- **الكاميرا**: 30 FPS
- **المعالجة**: 2 FPS (إطار واحد من كل 15)
- **زمن الاستجابة**: ~100-200ms لكل إطار

### استهلاك الموارد:
- **الذاكرة**: ~50 MB (النموذج 7 MB + معالجة)
- **CPU**: متوسط (تحويل الصورة + ML)
- **Battery**: استهلاك متوسط بسبب الكاميرا والمعالجة

## 🎨 قواعد التصنيف الحالية

| الإشارة | الحرف | الشرط |
|---------|-------|-------|
| ☝️ إصبع واحد | أ | `index && !middle && !ring && !pinky` |
| ✌️ إصبعين | ب | `index && middle && !ring && !pinky` |
| 🤟 3 أصابع | ج | `index && middle && ring && !pinky` |
| 🖐️ 4 أصابع | د | `index && middle && ring && pinky` |
| ✊ قبضة | ه | `!index && !middle && !ring && !pinky` |
| 🖐️ يد مفتوحة | و | `thumb && index && middle && ring && pinky` |
| 👍 إبهام | ي | `thumb && !index && !middle && !ring && !pinky` |

## 🔧 المشاكل المحتملة والحلول

### 1. لا تظهر النتيجة
**السبب:** التطبيق يعمل على Web/Windows
**الحل:** تشغيل على Android فقط

### 2. دقة منخفضة
**السبب:** الإضاءة سيئة أو يد بعيدة
**الحل:** 
- تحسين الإضاءة
- تقريب اليد من الكاميرا
- تقليل `minHandDetectionConfidence`

### 3. بطء في المعالجة
**السبب:** معالجة كل الإطارات
**الحل:** 
- زيادة `_skipFrames` (حالياً 15)
- استخدام `ResolutionPreset.low`

### 4. خطأ في التهيئة
**السبب:** ملف `hand_landmarker.task` غير موجود
**الحل:** التحقق من وجود الملف في `android/app/src/main/assets/`

## 📈 التحسينات المقترحة

### 1. زيادة عدد الحروف
```kotlin
// إضافة المزيد من القواعد
when {
    // حروف عربية كاملة (28 حرف)
    // حروف إنجليزية (A-Z)
    // أرقام (0-9)
}
```

### 2. استخدام Machine Learning متقدم
```kotlin
// تدريب نموذج TensorFlow Lite مخصص
// على dataset كبير من لغة الإشارة
```

### 3. كشف حركة اليد (Gestures)
```kotlin
// ليس فقط الوضع الثابت
// بل حركات اليد أيضاً (السحب، الدوران)
```

### 4. كشف يدين معاً
```kotlin
.setNumHands(2) // كشف يدين
// للإشارات التي تحتاج يدين
```

### 5. دعم الفيديو
```kotlin
.setRunningMode(RunningMode.VIDEO)
// للمعالجة الأسرع والأكثر سلاسة
```

## 🧪 طريقة الاختبار

### 1. تشغيل مع Logs
```bash
flutter run
adb logcat | grep -E "HandGesture|SignLanguage"
```

### 2. التحقق من النقاط
- ستظهر النقاط الزرقاء على يدك
- تأكد من ظهور 21 نقطة

### 3. تجربة الإشارات
```
☝️ → يجب أن يظهر "أ"
✌️ → يجب أن يظهر "ب"
✊ → يجب أن يظهر "ه"
```

## 📚 المراجع

1. [MediaPipe Hands Documentation](https://developers.google.com/mediapipe/solutions/vision/hand_landmarker)
2. [Flutter Platform Channels](https://docs.flutter.dev/development/platform-integration/platform-channels)
3. [Camera Plugin](https://pub.dev/packages/camera)

## 🎓 ملخص التقنية

**الميزات:**
- ✅ دقة عالية في كشف اليد (21 نقطة)
- ✅ معالجة في الوقت الفعلي
- ✅ لا يحتاج اتصال بالإنترنت
- ✅ يعمل على الهواتف منخفضة المواصفات

**القيود:**
- ❌ يعمل فقط على Android (ليس Web/iOS حالياً)
- ❌ يحتاج إضاءة جيدة
- ❌ عدد محدود من الإشارات (7 حروف حالياً)
- ❌ يد واحدة فقط

**الأداء:**
- ⚡ سريع (100-200ms للإطار)
- 💾 استهلاك معتدل للذاكرة
- 🔋 استهلاك متوسط للبطارية
