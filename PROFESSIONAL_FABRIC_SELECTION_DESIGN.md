# 🎨 تصميم احترافي لاختيار القماش

## 🎯 **الهدف:**
تطبيق تصميم احترافي مستوحى من صفحات المنتجات في المتاجر الإلكترونية لقسم اختيار القماش.

---

## ✨ **المميزات الجديدة:**

### 1. **عرض تفصيلي للقماش (Product Page Style)**

#### صفحة كاملة للقماش المختار تحتوي على:
- ✅ صورة كبيرة مع carousel للصور المتعددة
- ✅ أسهم تنقل بين الصور
- ✅ مؤشر الصور السفلي (dots)
- ✅ صور مصغرة قابلة للنقر
- ✅ معلومات تفصيلية عن القماش
- ✅ السعر الحالي + السعر الأصلي (إن وجد خصم)
- ✅ وصف القماش
- ✅ معلومات إضافية (النوع، الكمية المتاحة، ملاحظات)
- ✅ الألوان المتاحة (دوائر ملونة كبيرة)
- ✅ زر اختيار القماش

---

### 2. **Image Carousel (معرض الصور)**

```dart
PageView.builder(
  controller: _imageController,
  itemCount: images.length,
  onPageChanged: (index) {
    setState(() => _currentImageIndex = index);
  },
  itemBuilder: (context, index) {
    return _buildImage(images[index], cs);
  },
)
```

**المميزات:**
- ✅ صور متعددة للقماش
- ✅ أسهم تنقل يمين/يسار
- ✅ مؤشر الصورة الحالية (animated dots)
- ✅ صور مصغرة أسفل الصورة الرئيسية
- ✅ النقر على الصورة المصغرة للانتقال إليها
- ✅ أنيميشن سلس عند التنقل

---

### 3. **قسم التفاصيل**

#### أ) **معلومات المنتج:**
```
📦 اسم القماش (عنوان كبير)
💰 السعر (بارز وملون)
   ├─ سعر حالي (أحمر إذا كان هناك خصم)
   └─ سعر أصلي (مشطوب إذا كان هناك خصم)
```

#### ب) **الوصف:**
```
📝 وصف تفصيلي للقماش
   في صندوق ملون بخلفية رمادية فاتحة
```

#### ج) **معلومات إضافية:**
```
📌 معلومات في صناديق صغيرة:
   ├─ ℹ️ ملاحظات
   ├─ 📦 الكمية المتاحة
   └─ 🏷️ النوع
```

#### د) **الألوان المتاحة:**
```
🎨 دوائر ملونة كبيرة (48x48):
   ├─ ظل ملون (shadow)
   ├─ حدود رمادية
   └─ أيقونة للألوان الفاتحة جداً
```

---

### 4. **التخطيط المتجاوب (Responsive Layout)**

#### الشاشات الكبيرة (> 720px):
```
┌─────────────────────────────────────────┐
│  ┌───────────┐    ┌──────────────────┐ │
│  │           │    │  اسم القماش      │ │
│  │   الصورة  │    │  السعر           │ │
│  │           │    │  الوصف           │ │
│  │  (3/5)    │    │  معلومات         │ │
│  │           │    │  الألوان         │ │
│  └───────────┘    │  [زر الاختيار]  │ │
│  [صور مصغرة]     └──────────────────┘ │
└─────────────────────────────────────────┘
```

#### الشاشات الصغيرة (≤ 720px):
```
┌──────────────────┐
│  ┌────────────┐  │
│  │   الصورة   │  │
│  │            │  │
│  └────────────┘  │
│  [صور مصغرة]    │
│                  │
│  اسم القماش      │
│  السعر           │
│  الوصف           │
│  معلومات         │
│  الألوان         │
│  [زر الاختيار]  │
└──────────────────┘
```

---

### 5. **الكود الأساسي:**

#### أ) **PageController للصور:**
```dart
class _FabricStepState extends State<_FabricStep> {
  int _currentImageIndex = 0;
  final PageController _imageController = PageController();
  
  @override
  void dispose() {
    _imageController.dispose();
    super.dispose();
  }
}
```

#### ب) **بناء قسم الصور:**
```dart
Widget _buildImageSection(List<String> images, ColorScheme cs) {
  return Column(
    children: [
      // الصورة الرئيسية مع carousel
      AspectRatio(
        aspectRatio: 1,
        child: PageView + أسهم + مؤشر
      ),
      
      // الصور المصغرة
      SizedBox(
        height: 80,
        child: ListView صور مصغرة
      ),
    ],
  );
}
```

#### ج) **بناء قسم التفاصيل:**
```dart
Widget _buildDetailsSection(...) {
  return Column(
    children: [
      // الاسم
      Text(fabric['name']),
      
      // السعر
      Row([سعر حالي, سعر أصلي مشطوب]),
      
      // الوصف
      Container(description),
      
      // معلومات إضافية
      _buildInfoBox(...),
      
      // الألوان
      Wrap(دوائر ملونة),
      
      // زر الاختيار
      FilledButton.icon(...),
    ],
  );
}
```

---

### 6. **دعم الصور المتعددة:**

```dart
// جمع الصور من Firebase
final additionalImages = (fabric['additionalImages'] as List<dynamic>? ?? [])
    .map((e) => e.toString())
    .toList();
    
final images = <String>[
  fabric['imageUrl'] ?? '',
  ...additionalImages,
].where((img) => img.isNotEmpty).toList();
```

---

### 7. **أسهم التنقل:**

```dart
// السهم الأيمن
Positioned(
  right: 8,
  child: Container(
    decoration: BoxDecoration(
      color: Colors.white.withOpacity(0.9),
      shape: BoxShape.circle,
      boxShadow: [BoxShadow(...)],
    ),
    child: IconButton(
      icon: Icon(Icons.chevron_right),
      onPressed: () {
        if (_currentImageIndex < images.length - 1) {
          _imageController.nextPage(...);
        }
      },
    ),
  ),
)
```

---

### 8. **مؤشر الصور (Dots Indicator):**

```dart
Positioned(
  bottom: 16,
  child: Row(
    children: List.generate(
      images.length,
      (index) => Container(
        width: _currentImageIndex == index ? 24 : 8,
        height: 8,
        decoration: BoxDecoration(
          color: _currentImageIndex == index
              ? Colors.white
              : Colors.white.withOpacity(0.5),
          borderRadius: BorderRadius.circular(4),
        ),
      ),
    ),
  ),
)
```

---

### 9. **الصور المصغرة:**

```dart
ListView.separated(
  scrollDirection: Axis.horizontal,
  itemCount: images.length,
  itemBuilder: (context, index) {
    final isSelected = _currentImageIndex == index;
    return GestureDetector(
      onTap: () {
        _imageController.animateToPage(index, ...);
      },
      child: Container(
        width: 80,
        decoration: BoxDecoration(
          border: Border.all(
            color: isSelected ? cs.primary : cs.outlineVariant,
            width: isSelected ? 2.5 : 1,
          ),
          boxShadow: isSelected ? [shadow] : null,
        ),
        child: ClipRRect(
          child: _buildImage(images[index], cs),
        ),
      ),
    );
  },
)
```

---

### 10. **دوائر الألوان الكبيرة:**

```dart
Wrap(
  spacing: 12,
  runSpacing: 12,
  children: availableColors.map((colorData) {
    final color = Color(int.parse(colorHex.replaceFirst('#', '0xFF')));
    
    return Tooltip(
      message: colorName,
      child: Container(
        width: 48,
        height: 48,
        decoration: BoxDecoration(
          color: color,
          shape: BoxShape.circle,
          border: Border.all(
            color: Colors.grey.shade300,
            width: 2,
          ),
          boxShadow: [
            BoxShadow(
              color: color.withOpacity(0.4),
              blurRadius: 8,
              offset: Offset(0, 4),
            ),
          ],
        ),
        // أيقونة للألوان الفاتحة
        child: color.computeLuminance() > 0.9
            ? Icon(Icons.circle_outlined)
            : null,
      ),
    );
  }).toList(),
)
```

---

### 11. **صناديق المعلومات:**

```dart
Widget _buildInfoBox({
  required IconData icon,
  required String text,
  required ColorScheme cs,
  required TextTheme tt,
}) {
  return Container(
    padding: EdgeInsets.all(12),
    decoration: BoxDecoration(
      color: cs.surfaceContainerHighest,
      borderRadius: BorderRadius.circular(8),
      border: Border.all(color: cs.outlineVariant),
    ),
    child: Row(
      children: [
        Icon(icon, size: 20, color: cs.primary),
        SizedBox(width: 12),
        Expanded(
          child: Text(
            text,
            style: tt.bodyMedium?.copyWith(color: cs.onSurface),
          ),
        ),
      ],
    ),
  );
}
```

---

### 12. **زر التحديد:**

```dart
FilledButton.icon(
  onPressed: () {
    widget.onTypeChanged(
      fabric['name'],
      fabric['imageUrl'],
      fabric['id'],
    );
    setState(() => _showDetailView = false);
    HapticFeedback.lightImpact();
  },
  icon: Icon(Icons.check_circle),
  label: Text('اختيار هذا القماش'),
  style: FilledButton.styleFrom(
    padding: EdgeInsets.symmetric(vertical: 16),
    textStyle: tt.titleMedium?.copyWith(fontWeight: FontWeight.bold),
  ),
)
```

---

## 🎨 **التصميم البصري:**

### الألوان:
```
الخلفية: cs.surface
الصورة: cs.surfaceContainerHighest
الحدود: cs.outlineVariant
الحدود النشطة: cs.primary
السعر: cs.primary (أو cs.error للخصم)
الوصف: cs.surfaceContainerHighest
```

### الظلال:
```
الصورة الرئيسية:
  color: Colors.black.withOpacity(0.1)
  blurRadius: 20
  offset: Offset(0, 10)

الأسهم:
  color: Colors.black.withOpacity(0.2)
  blurRadius: 8

الألوان:
  color: color.withOpacity(0.4)
  blurRadius: 8
  offset: Offset(0, 4)
```

### الحواف المستديرة:
```
الصورة الرئيسية: 16
صناديق المعلومات: 8-12
الصور المصغرة: 11-12
```

---

## 🔄 **آلية العمل:**

### 1. عرض الشبكة (Grid View):
```
المستخدم يختار قماش من الشبكة
  ↓
يضغط زر "عرض التفاصيل"
  ↓
تظهر صفحة كاملة بتفاصيل القماش
```

### 2. عرض التفاصيل (Detail View):
```
صفحة المنتج الكاملة
  ↓
carousel للصور + تفاصيل كاملة
  ↓
يضغط "اختيار هذا القماش"
  ↓
يعود للشبكة مع تحديد القماش
```

### 3. التنقل بين الصور:
```
طرق التنقل:
  1. السهم الأيمن/الأيسر
  2. السحب (swipe)
  3. النقر على الصورة المصغرة
```

---

## ✅ **الفوائد:**

### 1. **تجربة مستخدم احترافية:**
- ✅ تصميم شبيه بالمتاجر الإلكترونية الكبرى
- ✅ صور متعددة بجودة عالية
- ✅ معلومات مفصلة وواضحة
- ✅ ألوان كبيرة وواضحة

### 2. **سهولة الاستخدام:**
- ✅ عرض شبكة بسيط للتصفح السريع
- ✅ عرض تفاصيل كامل للفحص الدقيق
- ✅ التنقل بين الصور بطرق متعددة
- ✅ معلومات منظمة ومرتبة

### 3. **متجاوب تماماً:**
- ✅ يعمل على جميع أحجام الشاشات
- ✅ تخطيط مختلف للشاشات الكبيرة/الصغيرة
- ✅ صور بجودة محسّنة (memCache)
- ✅ lazy loading للصور

### 4. **أداء ممتاز:**
- ✅ CachedNetworkImage للصور
- ✅ memCache للتحكم بالذاكرة
- ✅ PageController يدير الانتقالات
- ✅ أنيميشن سلس (300ms)

---

## 📱 **المظهر النهائي:**

### قبل (Old Design):
```
┌────────────┐  ┌────────────┐
│   قطن      │  │  ساتوش     │
│  [صورة]    │  │  [صورة]    │
│  السعر     │  │  السعر     │
└────────────┘  └────────────┘
```

### بعد (New Professional Design):
```
┌───────────────────────────────────────────┐
│  ┌──────────────┐     ┌─────────────────┐ │
│  │              │     │ قطن ياباني فاخر │ │
│  │   [صورة 1]   │     │ ر.ع 5.950      │ │
│  │              │     │ ⚊ر.ع 7.438⚊   │ │
│  │   ← ↓→        │     │                 │ │
│  └──────────────┘     │ 📝 قطن فاخر...  │ │
│  [🖼️][🖼️][🖼️]         │                 │ │
│                       │ ℹ️ ملاحظات...   │ │
│                       │ 📦 متوفر: 50    │ │
│                       │ 🏷️ النوع: قطن  │ │
│                       │                 │ │
│                       │ 🎨 الألوان:     │ │
│                       │ ⭕⭕⭕⭕⭕      │ │
│                       │                 │ │
│                       │ [✓ اختيار]     │ │
│                       └─────────────────┘ │
└───────────────────────────────────────────┘
```

**تصميم احترافي وجميل! 🎉**

